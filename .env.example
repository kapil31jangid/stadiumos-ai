# GCP Config
GCP_PROJECT=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Local Mock Flag
# Set to 'true' to run the backend in standalone mock mode (no live GCP APIs needed)
USE_MOCKS=true

# Server Config
PORT=8080
HOST=0.0.0.0
